{
  "name": "reels-project",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "cd frontend && npm ci",
    "build": "cd frontend && npm ci && npm run build"
  },
  "engines": {
    "node": "24.x"
  }
}

